<% amount = @carts_products.where(product_id: @product.id).count %>
$('.product[rel="<%= @product.id %>"] #amount').html('<%= amount %>')
$('.product[rel="<%= @product.id %>"] #full-price').html('<%= amount * @product.price %>')
$('span#total').html('<%= @cart.total %>')
